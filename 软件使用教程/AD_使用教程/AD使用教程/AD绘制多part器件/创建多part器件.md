## 多part元器件绘制    
1、创建原理图库文件，file-》library-》schematic library    
> 注意这个东西只是创建一个原理图器件的库,真正的原理图要进sch library里面来查找   

2、打开sch library,创建一个器件,在创建的文件菜单栏上，tools-》new component   
3、点击sch library中的new component(刚刚创建的器件),tools->new part   
3、就会在创建文件下，新增partA，partB等   
4、在part文件上绘制器件即可